# include <stdio.h>
#include <string.h>
//Task explore all library funtions//
void printString(char arr[]);
int countLength(char arr[]);

int main(){
char oldStr[] = "oldStr";
char newStr[] = "newStr";
strcpy(newStr, oldStr);
puts(newStr);



}